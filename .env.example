# MongoDB Connection Configuration
# Copy this file to .env and update the values as needed

# MongoDB Host (default: localhost)
DB_HOST=localhost

# MongoDB Port (default: 27017)
DB_PORT=27017

# Admin credentials (required if MongoDB authentication is enabled)
# Leave empty if running MongoDB without authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_admin_password

# Authentication Database (default: admin)
# The database where the admin user is stored
AUTH_DB=admin

# Example configurations for different environments:

# Local development (no auth)
# DB_HOST=localhost
# DB_PORT=27017
# ADMIN_USERNAME=
# ADMIN_PASSWORD=
# AUTH_DB=admin

# Local development (with auth)
# DB_HOST=localhost
# DB_PORT=27017
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=secretpassword
# AUTH_DB=admin

# Remote MongoDB instance
# DB_HOST=your-mongodb-server.com
# DB_PORT=27017
# ADMIN_USERNAME=your_username
# ADMIN_PASSWORD=your_password
# AUTH_DB=admin

# MongoDB Atlas (cloud)
# For Atlas, you might need to use the full connection string format
# Check MongoDB Atlas documentation for specific connection details
# DB_HOST=cluster0.xxxxx.mongodb.net
# DB_PORT=27017
# ADMIN_USERNAME=your_atlas_username
# ADMIN_PASSWORD=your_atlas_password
# AUTH_DB=admin