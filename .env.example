# MongoDB Connection Configuration
# Copy this file to .env and update the values as needed

# FOR MONGODB ATLAS (Cloud) - Use MONGODB_URI
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=YourApp

# FOR LOCALHOST AND VPS MONGODB - Use individual settings
# MongoDB Host (default: localhost)
DB_HOST=localhost

# MongoDB Port (default: 27017)
DB_PORT=27017

# Admin credentials (required if MongoDB authentication is enabled)
# Leave empty if running MongoDB without authentication
# IMPORTANT: If password contains special characters (#, !, @, %, etc.), wrap it in quotes
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_admin_password
# Example with special characters: ADMIN_PASSWORD="pX3#vB7t!qZ@9LjR%fNc"

# Authentication Database (default: admin)
# The database where the admin user is stored
AUTH_DB=admin

# Example configurations:

# MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=YourApp

# Localhost development (no auth)
# DB_HOST=localhost
# DB_PORT=27017
# ADMIN_USERNAME=
# ADMIN_PASSWORD=
# AUTH_DB=admin

# Localhost development (with auth) 
# DB_HOST=localhost
# DB_PORT=27017
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=secretpassword
# AUTH_DB=admin

# VPS/Remote MongoDB server (with special characters in password)
# DB_HOST=your-server-ip
# DB_PORT=27017
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD="your_complex_password#with!special@chars%"
# AUTH_DB=admin